import Layout from "../components/Layout/Layout";

const Auth = () => {
  return (
    <Layout
      // leftSlot={<BaseList />}
      centerSlot={"AUTH"}
      // rightSlot={<BaseAction />}
    />
  );
}

export default Auth;